
```markdown
# 📤 آپلود سنتر – سیستم مدیریت فایل پیشرفته

نسخه: 1.0  
برند: **سروهاست**  
پلتفرم: Laravel 10+  
لایسنس: محفوظ برای فروش — استفاده شخصی یا تجاری فقط با خرید مجوز مجاز است.

---

## 🌟 ویژگی‌های اصلی

- آپلود ساده برای مهمان‌ها و کاربران ثبت‌نام‌شده  
- پنل کاربری و مدیریت فایل‌های آپلود شده  
- پنل ادمین کامل با مدیریت کاربران، فایل‌ها، تبلیغات و FTP  
- تنظیمات پویا از طریق صفحه مدیریت (عنوان سایت، لوگو، ایمیل، محدودیت‌ها، SMTP و...)  
- پشتیبانی از رمزگذاری فایل‌های خصوصی  
- پیش‌نمایش آنلاین فایل‌ها (عکس، ویدیو، PDF و...)  
- سازگار با هاست‌های رایج (cPanel, DirectAdmin)  
- طراحی واکنش‌گرا و کاربرپسند

---

## 🛠️ نیازمندی‌های سرور

قبل از نصب، مطمئن شوید سرور شما این مشخصات را دارد:

- **سیستم عامل**: لینوکس (CentOS, Ubuntu, Debian)
- **Web Server**: Apache 2.4+ یا Nginx
- **PHP**: نسخه **8.0 یا بالاتر**
  - اکستنشن‌های ضروری:
    - `OpenSSL`
    - `PDO`
    - `Mbstring`
    - `Tokenizer`
    - `XML`
    - `Ctype`
    - `JSON`
    - `Fileinfo`
- **MySQL**: نسخه 5.7+ یا MariaDB 10.3+
- **حافظه PHP**: حداقل `128M`
- **فضای دیسک**: حداقل 100 مگابایت (برای فایل‌های اصلی)

> 💡 در cPanel یا DirectAdmin: از بخش **Select PHP Version** اکستنشن‌ها را فعال کنید.

---

## 📥 راهنمای نصب دستی (بدون اینستالر)

این روش برای کاربرانی طراحی شده که دسترسی به cPanel یا DirectAdmin دارند.

### ۱. آپلود فایل‌ها

1. تمام محتویات پوشه `public` را در **ریشه دامنه** (مثلاً `public_html`) آپلود کنید.
2. سایر فایل‌ها (مثل `app`, `config`, `database`, `vendor`, ...) را **در یک پوشه خارج از public_html** آپلود کنید (مثلاً `/home/username/uploadcenter`).

📌 مثال ساختار:
```
/home/username/
├── uploadcenter/       ← فایل‌های اصلی لاراول
└── public_html/        ← فقط فایل‌های داخل پوشه public
```

---

### ۲. تنظیم `index.php`

فایل `public_html/index.php` را ویرایش کنید:

```php
// قبل:
require __DIR__.'/../vendor/autoload.php';
$app = require_once __DIR__.'/../bootstrap/app.php';

// بعد (مسیر را مطابق محل آپلود اصلی اصلاح کنید):
require __DIR__.'/../uploadcenter/vendor/autoload.php';
$app = require_once __DIR__.'/../uploadcenter/bootstrap/app.php';
```

---

### ۳. ایجاد فایل `.env`

در پوشه اصلی اسکریپت (جایی که `artisan` هست)، یک فایل به نام `.env` ایجاد کنید:

```env
APP_NAME="آپلود سنتر"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://yourdomain.com

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your@email.com
MAIL_PASSWORD=your_app_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@yourdomain.com"
MAIL_FROM_NAME="آپلود سنتر"
```

> 🔑 فیلد `APP_KEY` را **خالی بگذارید** — در مرحله بعد پر می‌شود.

---

### ۴. تولید کلید امنیتی

از طریق **Terminal در cPanel** یا **SSH**، دستور زیر را اجرا کنید:

```bash
cd /home/username/uploadcenter
php artisan key:generate
```

> ❌ اگر دسترسی SSH ندارید، از بخش **Cron Jobs** در cPanel استفاده کنید:
> ```
> php /home/username/uploadcenter/artisan key:generate
> ```

---

### ۵. اجرای میگریشن

همین‌طور دستور زیر را اجرا کنید تا جداول دیتابیس ساخته شوند:

```bash
php artisan migrate --force
```

---

### ۶. ایجاد ادمین اولیه

1. وارد **phpMyAdmin** شوید.
2. در جدول `users` یک رکورد جدید اضافه کنید:

| فیلد | مقدار |
|------|--------|
| `name` | "مدیر سیستم" |
| `email` | admin@yourdomain.com |
| `password` | [هش BCRYPT رمز عبور شما] |
| `role` | `admin` |
| `email_verified_at` | `2024-01-01 00:00:00` |

> 🔐 برای هش کردن رمز عبور، از این کد کمکی استفاده کنید:
> ```php
> <?php echo password_hash('your_password', PASSWORD_BCRYPT); ?>
> ```

---

### ۷. تنظیمات htaccess (Apache)

مطمئن شوید فایل `public_html/.htaccess` این محتوا را دارد:

```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
```

---

## 🔒 نکات امنیتی

- ✅ **هرگز فایل `.env` را در معرض دید عموم قرار ندهید.**
- ✅ `APP_DEBUG` را در محیط تولید حتماً روی `false` تنظیم کنید.
- ✅ دسترسی پوشه‌های `storage` و `bootstrap/cache` را روی `755` تنظیم کنید.
- ✅ از رمزهای عبور پیچیده برای دیتابیس و ادمین استفاده کنید.
- ✅ در صورت استفاده از Gmail برای SMTP، از **App Password** استفاده کنید (نه رمز اصلی).

---

## 📞 پشتیبانی

این محصول فقط با خرید مجوز رسمی قابل استفاده است.  
هرگونه بازنشر، کپی یا استفاده غیرقانونی ممنوع است.

برای پشتیبانی فنی، لطفاً از طریق ایمیل با ما تماس بگیرید:  
✉️ **support@sarvohost.com**

---

> © 2025 سروهاست — همه حقوق محفوظ است.
```

---

### ✅ نکته نهایی:
این `README.md` را در **ریشه پروژه** ذخیره کن تا خریدار بلافاصله بعد از استخراج فایل‌ها، راهنمای نصب را مشاهده کند.

اگر خواستی نسخه PDF یا نسخه فارسی کامل‌تر (با عکس) هم برات آماده کنم، فقط بگو!  
موفق باشی و فروش پرباری داشته باشی! 💙
